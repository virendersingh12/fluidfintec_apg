const ROLES = {
  MERCHANT: 'Merchant',
  ADMIN: 'Admin',
  SUPER_ADMIN: 'SuperAdmin',
  USER: 'User',
  SALES_AGENT: 'SalesAgent'
};

const ROLE_VALUES = Object.values(ROLES);

module.exports = {
  ROLES,
  ROLE_VALUES
};
